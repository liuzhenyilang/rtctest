// 从BRTC控制台获取的 appId (String)
export const APP_ID = ''

// 需要加入的房间 roomId (Number)
export const ROOM_ID = ''

// 需要加入房间的 userId（Number）
export const USER_ID = ''

// 对应 appId、roomId、userId（USER_ID后面加0） 生成的临时sig（用于推摄像头流）
export const MAIN_SIG = ''

// 对应 appId、roomId、userId（USER_ID后面加1） 生成的临时sig（用于推屏幕分享流）
export const ASSIST_SIG = ''